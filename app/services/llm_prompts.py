TELEGRAM_MARKDOWN_RULES = """
Формат ответа — Markdown для Telegram (будет преобразован в красивый вид):
- Заголовки: **жирным**, без символов #
- Списки: только маркеры "- ", НЕ используй таблицы с символом |
- Код: блоки ```python ... ``` или инлайн `код`
- Короткие абзацы, эмодзи уместны
- ЗАПРЕЩЕНО: HTML-теги (<b>, <code>, <pre> и любые другие) — только Markdown
- ЗАПРЕЩЕНО: символ · и строки из одних дефисов (----------)
- Не копируй docstring целиком (Parameters, Returns, ----------).
""".strip()

DEBUG_CODE_RULES = """
Разбор кода — коротко, по шагам:
1) **Что не так** (1–3 пункта "- ")
2) **Как исправить** (1–3 пункта "- ")
3) **Исправленный код** — один блок ```python ... ```

Не повторяй docstring, Parameters, Returns и линии из дефисов.
Не выдумывай код, которого нет у студента. Только Markdown.
""".strip()
